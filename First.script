

for (; "udlre" != syscall(getbutton,"");)
{
    }

syscall(cleardisplay, " ");
syscall(setcursorstartx, "0"); syscall(setcursorstarty, "0");
syscall (drawtext, "Hello Doodi!");

for (; "udlre" == syscall (getbutton, "");)
{}